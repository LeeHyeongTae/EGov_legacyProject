<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bitcamp.web.mappers.MemberMapper">
  <!--   <select id="list" resultType="domains.MemberDTO">
        select
            bicycleSeq, title, image, year, price, manufacturer, kind
        from member
    </select>  -->
</mapper>